{
  "name": "text-pixel-art",
  "version": "1.0.0",
  "description": "文字像素画 - 将文字转换为创意像素画",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}

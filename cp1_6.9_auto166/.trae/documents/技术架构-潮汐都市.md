## 1. 架构设计

```mermaid
graph TD
    subgraph "Frontend 前端层"
        A["index.html 入口"] --> B["App.tsx 根组件"]
        B --> C["工具栏 Toolbar"]
        B --> D["CityWaves.tsx 波浪图组件"]
        B --> E["TidePanel.tsx 信息面板组件"]
        D --> F["Canvas 2D 渲染引擎"]
        E --> G["MiniCanvas 预测曲线"]
    end
    subgraph "State 状态层"
        B --> H["useState + useEffect<br/>全局状态管理"]
        H --> H1["城市潮汐数据数组"]
        H --> H2["选中城市ID"]
        H --> H3["播放速度系数"]
        H --> H4["暂停状态"]
    end
    subgraph "Utils 工具层"
        I["tideUtils.ts<br/>潮汐计算工具"]
        J["colorUtils.ts<br/>颜色工具"]
        K["canvasUtils.ts<br/>Canvas渲染工具"]
    end
    subgraph "Data 数据层"
        L["cities.ts<br/>20城市配置数据"]
    end
    D --> I
    D --> J
    D --> K
    E --> I
    E --> G
    B --> L
```

## 2. 技术描述

- **前端框架**：React@18 + TypeScript
- **构建工具**：Vite@5 + @vitejs/plugin-react
- **渲染引擎**：Canvas 2D API（原生，无额外图形库）
- **状态管理**：React useState/useEffect（轻量场景无需zustand）
- **样式方案**：原生CSS + CSS变量（深色主题统一token）
- **初始化方式**：npm create vite-init@latest（react-ts模板）

